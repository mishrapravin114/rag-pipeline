# Environment Variables Configuration Example
# Copy this file to .env and update with your values

# ===========================================
# Database Configuration
# ===========================================

# MySQL root password (only used for initial setup)
MYSQL_ROOT_PASSWORD=your-secure-root-password

# MySQL application user credentials
MYSQL_USER=fda_user
MYSQL_PASSWORD=your-secure-password

# Database URL (automatically set in Docker, override for external DB)
# DATABASE_URL=mysql+pymysql://user:password@host:port/database

# Database Connection Pool Settings
# These control how the application manages database connections
# Higher values = more concurrent connections but more memory usage
SQLALCHEMY_POOL_SIZE=15
SQLALCHEMY_MAX_OVERFLOW=30
SQLALCHEMY_POOL_TIMEOUT=60
SQLALCHEMY_POOL_RECYCLE=3600
SQLALCHEMY_POOL_PRE_PING=true
SQLALCHEMY_ECHO_POOL=false

# ===========================================
# API Keys and Secrets
# ===========================================

# Google Gemini
GOOGLE_API_KEY=your_google_api_key_here

# Google OAuth
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here
GOOGLE_CLIENT_SECRETS_FILE=your_google_client_secrets_file_here
NEXT_PUBLIC_GOOGLE_APP_ID=your_google_app_id_here

# Secret key for JWT
SECRET_KEY=your_secret_key_here
FRONTEND_URL=http://localhost:3001
BACKEND_URL=http://localhost:8090

# ===========================================
# Frontend Configuration
# ===========================================

# API URL for frontend to connect to backend
# Local development:
NEXT_PUBLIC_API_URL=http://localhost:8090

# Production with reverse proxy:
# NEXT_PUBLIC_API_URL=http://yourdomain.com

# Production without reverse proxy:
# NEXT_PUBLIC_API_URL=http://your-ip:8090

# ===========================================
# Optional Configuration
# ===========================================

# Logging level (DEBUG, INFO, WARNING, ERROR)
# LOG_LEVEL=INFO

# Enable debug mode
# DEBUG=false

# ChromaDB persistence directory (default: ./backend/data/chromadb)
# CHROMADB_PATH=/path/to/chromadb

# Upload size limits (in MB)
# MAX_UPLOAD_SIZE=100

# Processing configuration
# MAX_WORKERS=3
# BATCH_SIZE=10

# Database Pool Configuration (Advanced)
# SQLALCHEMY_POOL_SIZE=15         # Base number of connections in pool
# SQLALCHEMY_MAX_OVERFLOW=30      # Additional connections beyond pool_size
# SQLALCHEMY_POOL_TIMEOUT=60      # Seconds to wait for connection from pool
# SQLALCHEMY_POOL_RECYCLE=3600    # Seconds before connection is recycled
# SQLALCHEMY_POOL_PRE_PING=true   # Validate connections before use
# SQLALCHEMY_ECHO_POOL=false      # Enable pool debugging (logs connection events)

# Session timeout (in minutes)
# SESSION_TIMEOUT=60

# CORS allowed origins (comma-separated)
# CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# ===========================================
# Production-Only Settings
# ===========================================

# SSL/TLS Configuration (if not using reverse proxy)
# SSL_CERT_PATH=/path/to/cert.pem
# SSL_KEY_PATH=/path/to/key.pem

# Redis URL (if using Redis for caching/sessions)
# REDIS_URL=redis://localhost:6379

# Sentry DSN (for error tracking)
# SENTRY_DSN=https://your-sentry-dsn

# ===========================================
# Development-Only Settings
# ===========================================

# Hot reload for backend
# RELOAD=true

# Frontend development server
# NEXT_TELEMETRY_DISABLED=1